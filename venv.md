How to create a Python virtual environment
==========================================

```bash
mkdir tmp/newdir
cd tmp/newdir
python -mvenv venv
source venv/bin/activate
which python3
pip install -r requirements.txt
```